
  # Security Attendance Management App

  This is a code bundle for Security Attendance Management App. The original project is available at https://www.figma.com/design/yuHt7QkMw1RPmuHaMw391d/Security-Attendance-Management-App.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  